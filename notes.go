package main

import "fmt"

func muhammad() {
	fmt.Println("hello world")

	// Instead of holding the value directly in the variable, we hold an address of the location in memory that holds the variable.

	// Our variable is going to point to another location that's going to hold the information that we're interested in.
}
